name: 01 - Building Blocks
on: 
  push:
  workflow_dispatch:
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
      - echo: 'Hello, world!'
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
      - echo: 'I |
               will |
               fail'
      - name: Say goodby
      - echo: 'Goodbye!'
      - echo: 'Goodbye!'
  